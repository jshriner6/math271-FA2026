<?xml version="1.0" encoding="UTF-8"?>

    <worksheet margin="1in" bottom="1.25cm" xml:id="euler-complete">
    <title>Euler's Formula (Complete)</title>
        <page>
        
            <exercise workspace="2in">
            <title>Motivation</title>
                <statement>
                    <p>
                
                    </p>
                </statement>
                <solution>
                    <p>
                        The standard form of a complex number can be difficult to compute with (pick your favorite complex 
                        number with non-zero real and imaginary parts and try computing <m>z^5</m>!). It is also not clear in this
                        form why complex numbers are ideal for representing waves. We'll see another representation for complex
                        numbers, and an important connection between this representation and wave functions.
                    </p>
                </solution>
            </exercise>

            <exercise workspace="4in" >
            <title>Polar Coordinate Representation</title>
                <statement>
                    <p>
                        In standard coordinates, we identify a complex number <m>z</m> uniquely by its real and imaginary parts.
                        <md>
                            z= a + bi , \,\,\,\, z = (a,b)
                        </md>
                        
                        This is not the only way to identify a complex number.
                    </p>
                </statement>
                <solution>
                    <p>
                        We can also identify a complex number by specifying its modulus <m>r</m> and the angle <m>\theta</m> (in radians) formed
                        from the positive real axis. This in general would not be unique, so it is common convention to require that
                        <m>-\pi \lt \theta \leq \pi</m>
                    </p>
                    <p>
                        Using trigonometry, we can make the connection between standard form and polar form:
                        <md>
                            \text{Re}(z) = r \cos(\theta), \,\,\, \text{Im}(z) = r \sin(\theta)
                        </md>.
                        
                    </p>
                </solution>
            </exercise>
    
    </page>
    <page>

            <exercise>
                <title>Example: Polar Coordinate Representation</title>
                <introduction>
                    <p>
                        
                    </p>
                </introduction>
                <task workspace="2in">
                    <statement>
                        <p>
                            Let <m>z=1-i</m>.
                        </p>
                    </statement>
                    <solution>
                        <p>
                            The modulus would be computed as 
                            <md>
                                r = |z| = \sqrt{1^2 +(-1)^2} = \sqrt{2}
                            </md>.
                            We can use the relationships between the real and imaginary parts to the angle <m>\theta</m>
                            to determine that
                            <md>
                                \tan(\theta) = -1
                            </md>.
                            An intuitive first attempt would be to solve <m>\theta = \arctan(-1) = -\frac{\pi}{4}</m>. In
                            this example that is the correct answer, but in general we must be aware of how the arctangent 
                            function is defined. It is the inverse function of tangent on the restricted domain <m>(-\pi/2, \pi/2)</m>,
                            which means the range of the arctangent function is <m>(-\pi/2, \pi/2)</m> (see the <url href="https://www.desmos.com/calculator/4pqck8nn47">Desmos graph</url> presented in class for a visual). If our angle should ever be outside
                            of this range, we'll need to adjust it accordingly by adding or subtracting <m>\pi</m>.
                            
                        </p>
                    </solution>
                </task>

                <task workspace="2in">
                    <statement>
                        <p>
                            Let <m>z= - 1+i</m>.
                        </p>
                    </statement>
                    <solution>
                        <p>
                            The modulus is computed in the same way as the previous example. However, plotting the complex number shows
                            this should be in the second quadrant, but arctangent would give an angle in the fourth quadrant. We can 
                            get the correct angle by computing 
                            <md>
                                \theta = \arctan(-1) + \pi = \frac{3\pi}{4}
                            </md>.
                            
                        </p>
                    </solution>
                </task>
                
                
            </exercise>
        </page>

        <page>
            <exercise>
            <title>Euler's Formula</title>
                <statement>
                    <p>
                        Recall
                        <md>
                            a + bi = r \cos(\theta) + ir\sin(\theta) = r(\cos(\theta) + i \sin(\theta) )
                        </md>.
                        
                    </p>
                </statement>
                <solution>
                    <p>
                        Define <m>f(\theta) = \cos(\theta) + i \sin(\theta)</m>. Then using derivative rules from calculus 1, we
                        can compute
                    </p>
                    <p>
                        <m>
                            \begin{align*}
                            f'(\theta) \amp= -\sin(\theta) +i \cos(\theta) \\
                                        \amp= i^2\sin(\theta) + i \cos(\theta) \\
                                        \amp= i(\cos(\theta)+ i\sin(\theta)) \\
                                        \amp= if(\theta)
                            \end{align*}
                        </m>
                    </p>
                    <p>

                        This is a differential equation, but one that we can solve if we think about what it is saying. If
                        we ignored the constant <m>i</m>, we'd be looking for a function whose derivative is the function itself. 
                        This is why the number <m>e</m> is so important in calculus! The exponential function with base <m>e</m>
                        is its own derivative; in symbols,
                        <md>
                            \frac{d}{dx}(e^x) = e^x
                        </md>.
                        Now if we remember the chain rule, we can adjust for the constant <m>i</m> to see that 
                        <md>
                            f(\theta) = Ce^{i\theta}
                        </md>
                        satisfies
                        <md>
                            f'(\theta) = if(\theta)
                        </md>.
                        Further, our original definition of <m>f</m> satisfies <m>f(0) = 1</m>, so this means <m>C=1</m>, or 
                        <m>f(\theta)= e^{i\theta}</m>. Equating our original definition of <m>f</m> with this solution to 
                        the differential equation yields <term>Euler's Formula</term>:
                        <md>
                            e^{i\theta}= \cos(\theta) + i\sin(\theta)
                        </md>.
                        Euler's formula gives us an exponential form for complex numbers, and is a very useful form for 
                        efficient representation and computation:
                        </p>
                        <p>
                        <m>
                            \begin{align*}
                            z \amp= a + bi \\
                                \amp= r\cos(\theta) + r\sin(\theta)i \\
                                \amp= r(\cos(\theta) + \sin(\theta)i) \\
                                \amp=re^{i\theta}
                            \end{align*}
                        </m>
                        
                    </p>
                </solution>
                
            </exercise>
            
        </page>

    </worksheet>
